#include <stdio.h>
void main() {
    int a,b,c;
    printf("Enter Number a: ");
    scanf("%d",& a);
     if (a>0){
        printf("%d is Possitive",a);
    }
    else {
        printf("%d is Negative",a);
    }
}